import { Geist, Inclusive_Sans, Instrument_Serif } from "next/font/google";

export const geist = Geist({ subsets: ["latin"] });

export const inclusive_sans = Inclusive_Sans({
  weight: ["400"],
  subsets: ["latin"],
});

export const instrument_serif = Instrument_Serif({
  weight: ["400"],
  subsets: ["latin"],
});
