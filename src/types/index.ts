export interface User {
  name: string;
  image: string;
  username: string;
  bio: string;
  about: string;
  website: string;
  location: string;
}

export interface Contact {
  platform: string;
  username: string;
  url: string;
}
